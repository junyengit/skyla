{
  "name": "skyla",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.4.0-canary.1",
  "engines": {
    "bun": ">=1.4.0-canary.1",
    "node": "24.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "trustedDependencies": [
    "sharp",
    "unrs-resolver"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "bun run test:unit",
    "test:unit": "turbo test:unit",
    "test:smoke": "node scripts/smoke/route-smoke.mjs",
    "security": "bun run security:artifacts && bun run security:audit",
    "security:artifacts": "node scripts/security/check-tracked-artifacts.mjs",
    "security:audit": "bun audit --audit-level=high",
    "check": "turbo lint typecheck test:unit build && bun run security:artifacts",
    "web:dev": "bun run --cwd apps/web dev",
    "web:build": "bun run --cwd apps/web build",
    "web:start": "bun run --cwd apps/web start"
  },
  "devDependencies": {
    "turbo": "2.10.1",
    "vitest": "4.1.9"
  }
}
