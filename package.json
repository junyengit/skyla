{
  "name": "skyla",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.4.0-canary.1",
  "engines": {
    "bun": ">=1.4.0-canary.1",
    "node": "24.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "trustedDependencies": [
    "sharp",
    "unrs-resolver"
  ],
  "overrides": {
    "postcss": "8.5.16"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "bun run test:unit",
    "test:unit": "turbo test:unit && bun run convex:test:unit && bun run test:scripts",
    "test:scripts": "vitest run scripts",
    "test:smoke": "node scripts/smoke/route-smoke.mjs",
    "test:payments": "node scripts/smoke/payment-api-smoke.mjs",
    "test:production-readiness": "node scripts/smoke/production-readiness-smoke.mjs",
    "test:supabase-retired:live": "node scripts/smoke/supabase-retirement-smoke.mjs",
    "test:acceptance:linked": "node scripts/smoke/linked-acceptance-smoke.mjs",
    "test:acceptance:preflight": "SKYLA_ACCEPTANCE_PREFLIGHT=1 node scripts/smoke/linked-acceptance-smoke.mjs",
    "convex:test:unit": "vitest run convex",
    "convex:env:check": "node scripts/setup/check-convex-env.mjs",
    "vercel:env:check": "node scripts/setup/check-vercel-env.mjs",
    "dashboard:readiness": "node scripts/setup/check-dashboard-readiness.mjs",
    "convex:schema:typecheck": "tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --strict --skipLibCheck convex/schema.ts",
    "convex:functions:typecheck": "tsc -p convex/tsconfig.json --noEmit",
    "convex:codegen": "convex codegen --typecheck enable",
    "security": "bun run security:artifacts && bun run security:supabase-retired && bun run security:audit",
    "security:artifacts": "node scripts/security/check-tracked-artifacts.mjs",
    "security:supabase-retired": "node scripts/security/check-legacy-supabase-retirement.mjs",
    "security:audit": "bun audit --audit-level=high",
    "check": "turbo lint typecheck test:unit build && bun run convex:test:unit && bun run test:scripts && bun run convex:schema:typecheck && bun run convex:functions:typecheck && bun run security:artifacts && bun run security:supabase-retired",
    "web:dev": "bun run --cwd apps/web dev",
    "web:build": "bun run --cwd apps/web build",
    "web:start": "bun run --cwd apps/web start"
  },
  "devDependencies": {
    "turbo": "2.10.3",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  },
  "dependencies": {
    "@skyla/payments": "workspace:*",
    "convex": "^1.42.1"
  }
}
